import { EditorPanel } from './EditorPanel';
import { renderHook, act } from '@testing-library/react-hooks';

// Editor
// should add a card to the database
// should notify the result of saving a card
// should persist title and content during editing

test('should open the state', () => {
  expect(0).toBe(0);
});
